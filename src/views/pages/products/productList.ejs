<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../../partials/head.ejs') %>
    <title><%= title %></title>
</head>

<body>
    <header>
         <%- include('../../partials/header.ejs') %>
    </header>

    <main>

        <% for(let i=0;i < products.length;i++) {%>
        <div class='lista_productos'>
            <img class='imagen_lista_productos' src="/images/products/<%= products[i].secondImage %>">
            <div class="titulo_precio_descripcion">
                <div class="nombre_lista"><a href="/productos/<%= products[i].category %>/<%= products[i].id %>"><%=products[i].name%></a></div>
                <div class="precio_lista"><%=products[i].price%></div>
                <div class="descripcion_lista"><%=products[i].description%></div>
            </div>
            <button class="button-list">Agregar al carrito</button>
        </div>
        <% } %>

    </main>

    <footer>
        <%- include('../../partials/footer.ejs') %>
    </footer>
</body>

</html>